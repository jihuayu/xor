# xor-encryption macOS ARM64 Binary

This package contains the macOS ARM64 (Apple Silicon) binary for xor-encryption.

This package is automatically installed as an optional dependency of the main `xor-encryption` package.

**Do not install this package directly unless you know what you are doing.**
